export enum EActionType {
  Add = "add",
  Show = "show",
  Edit = "edit",
  Delete = "delete",
}

export enum ETableActionType {
  Button = "button",
  Dropdown = "dropdown",
}

export enum EAgainType {
  Un = "un",
  Une = "une",
}